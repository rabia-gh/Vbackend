pipeline {
 agent any
    stages {
        stage('Test') {
            steps {
                sh 'pwd'
                sh 'cd "$(pwd)"/app'
                sh 'ls'
                
            }
        }
    }
    }
